component {
	// Module Dependencies
	//this.dependencies 		= ['cbjavaloader'];

	function configure() {}

	/**
	* Fired when the module is registered and activated.
	*/
	function onLoad(){
		// javaloader setup
		//controller.getWireBox().getInstance( "loader@cbjavaloader" ).appendPaths( getDirectoryFromPath( getCurrentTemplatePath() ) & "lib"  );

	}
}